#include <stdio.h>
#include <stdbool.h>

// structure 
struct  moteur {

	bool démarer;
	bool utiliser;
	bool arreter;
};
// fontions demarer
void DémarerMoteur(struct moteur);
if (moteur->démarre) {
	moteur->déemarre = true;
	printf("Le moteur a été démarré.\n")
}
else {
	printf("le moteur est déja démaré.\n")
}
// fontion utiliser
void UtiliserMoteur(struct moteur);
//fontion arreter
void ArreterMoteur(struct moteur);
